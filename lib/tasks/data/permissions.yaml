---
#controller的名字均为程序中controller名称的单数，如AnalysesController为Analysis，必须有对应的model,可以建空model
-
  controller: day_pdts_controller
  actions: [index, show, new, create, edit, update, verify_index, verify_show, verifying, rejected, cmp_verifying, cmp_verify_index, cmp_verify_show, cmp_rejected, upreport]

-
  controller: day_pdt_rpts_controller
  actions: [index, show, sglfct_statistic, sglfct_stc_cau, radar_chart, new_quota_chart, static_pool, produce_report, xls_day_download]

-
  controller: mth_pdt_rpts_controller
  actions: [mth_rpt_create, index, show, edit, update, verifying, download_report, verify_index, verify_show, rejected, cmp_verifying, cmp_verify_index, cmp_verify_show, cmp_rejected, upreport, mth_report_finish_index, mth_report_finish_show, xls_mth_download]

-
  controller: analyses_controller
  actions: [compare, month_compare, area_time, area_time_compare]

-
  controller: reports_controller
  actions: [index, mth_report_show, day_report, mth_report, xls_day_download, xls_mth_download]
-
  controller: emp_infs_controller
  actions: [index, new, create, edit, update, destroy, watercms_flow, xls_download, parse_excel, grp_index, fct_edit, fct_update, fct_destroy, parse_fct_excel, create]

-
  controller: emp_effs_controller
  actions: [index, new, create, edit, update, destroy, watercms_flow, xls_download, parse_excel, grp_index, fct_edit, fct_update, fct_destroy, parse_fct_excel, create]

-
  controller: factories_controller
  actions: [edit, update]
